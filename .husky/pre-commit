#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Pre-commit hook for RAYA Backend
# Runs lint-staged with automated fixes and validation

echo "🔍 Running pre-commit checks..."

# Run lint-staged (defined in package.json)
npx lint-staged

if [ $? -ne 0 ]; then
  echo "❌ Pre-commit checks failed. Please fix the errors and try again."
  exit 1
fi

echo "✅ Pre-commit checks passed!"
exit 0
